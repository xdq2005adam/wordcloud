# Cloudflare Tunnel 配置文件
# 用于在 Railway 上部署 Cloudflare Tunnel

# 替换为你的隧道 ID（运行 cloudflared tunnel create wordcloud 后获得）
tunnel: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Railway 上的路径（不要修改）
credentials-file: /etc/cloudflared/credentials.json

# 路由配置
ingress:
  # 你的免费域名（替换为你的域名）
  - hostname: wordcloud-app.trycloudflare.com
    # 你的 Railway URL（替换为你的实际 URL）
    service: https://web-production-27bb4.up.railway.app
  
  # 捕获所有其他请求（404 错误）
  - service: http_status:404

